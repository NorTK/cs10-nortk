# Journal: 2026-05-12 (agent)
==============================

## [03:50:00]
**Tags**: GOT

Verified user updates to config.xml adding btrfs-progs to bootstrap and specifying x86_64 arch for grub modules. Ready for testing build.

## [03:58:54]
**Tags**: GOT,DEC

DEC: Removed 'insmod btrfs' from ISO grub templates instead of embedding it into the EFI core image. RAT: KIWI in UEFI mode copies the pre-signed grubx64.efi and skips copying modules to the ISO, so 'btrfs.mod' is never present. The ISO uses iso9660 anyway and does not need BTRFS. The installed system will correctly install btrfs modules via grub2-install during first boot/installation.

## [04:18:22]
**Tags**: DEC,RAT,PRB

DEC: Added 'grub2-efi-x64-cdboot' package to config.xml. RAT: KIWI was falling back to 'grubx64.efi' for the ISO bootloader, which lacks the 'iso9660' module and caused GRUB to drop to a shell. PRB: Hyperscale kernel fails secure boot verification because it is signed by a SIG key not natively trusted by CentOS shim.

## [04:20:31]
**Tags**: DEC,RAT

DEC: Changed firmware setting in config.xml from 'uefi' to 'efi' to disable the secure boot implementation requirement. RAT: This allows the Hyperscale kernel to load without a trusted shim signature and bypasses the need for custom MOK enrollment.

## [11:10:48]
**Tags**: DEC,RAT

DEC: Forced dynamic generation of the unsigned EFI core image. RAT: Standard RHEL/CentOS packages install a pre-compiled 'grubx64.efi' that skips BTRFS support. By deleting it in 'config.sh', we drive KIWI into using its fallback 'grub2-mkimage' code path which auto-injects native BTRFS modules into the image.

## [12:21:58]
**Tags**: GOT,DEC

GOT: Diagnosed installed system grub fallback failure as arising from build-time vs install-time partition UUID mismatches baked into dynamically generated KIWI EFI images. DEC: Refactored 'config.sh' to stage a full custom 'grubx64.efi' explicitly configured via relative paths to circumvent UUID reliance entirely.

## [12:44:57]
**Tags**: GOT,DEC

GOT: Discovered KIWI writes broken GRUB configs to the disk image at the absolute end of the pipeline, overwriting in-chroot work. DEC: Leveraged the 'editbootinstall' hook paired with 'mtools' direct partition writing via fdisk sector offset calculation to atomicaly purge static UUIDs right before packaging. Fully robust fix.

