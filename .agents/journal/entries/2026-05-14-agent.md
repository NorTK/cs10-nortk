# Journal: 2026-05-14 (agent)
==============================

## [20:14:07]
**Tags**: GOT,DEC

Fixing lexer errors by reverting 191766 to $ and improving BTRFS/RAID support in GRUB. Updated config.sh to include RAID modules in the core image and stage all modules for EFI partition injection. Refined fix-efi-stub.sh to inject modules and improved the dynamic config stub for subvolume awareness.

## [21:43:06]
**Tags**: DEC,RAT

Set btrfs_set_default_volume to false in config.xml to ensure GRUB can find sibling subvolumes (like 'boot') from the top-level (ID 5). Updated scripts/fix-efi-stub.sh with more robust search paths in the dynamic stub.

## [21:49:20]
**Tags**: DEC,RAT

Hardened BTRFS boot support by adding 'zstd' module to custom GRUB image and KIWI config. Refactored scripts/fix-efi-stub.sh to explicitly inject our BTRFS-enabled 'grubx64.efi' into the EFI partition, ensuring the bootloader can read the subvolumes immediately.

## [21:51:29]
**Tags**: DEC,RAT

Refactored GNUmakefile to separate 'prepare' and 'create' phases for the disk image build. This aligns with KIWI best practices and allows for more granular control over the build pipeline.

